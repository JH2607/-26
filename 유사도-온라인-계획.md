# 유사도 이중 모드 계획

> 지금 HTML 로직은 **오프라인 폴백**으로 유지한다.  
> 네트워크가 되면 **의미 임베딩**으로 채점하고, 끊기면 자동으로 지금 로직으로 돌아간다.

---

## 1. 목표

- 표에 없는 단어도 “관련 있으면” 점수가 오를 것 (`컴데인` + `데이터과학과`)
- `1905`처럼 다의어는 연도 축·밴드 축 둘 다 살릴 것
- 같은 입력은 같은 점수 (재현 가능)
- API 키가 HTML에 노출되지 않을 것
- 와이파이 없어도 행사는 진행될 것

비목표: 세상의 모든 유사도를 완벽하게 재현하는 것. 부스에서 **탐색이 되게** 만드는 것.

---

## 2. 왜 HTML에서 바로 OpenAI를 부르면 안 되나

브라우저에 키를 넣으면 참가자가 개발자 도구로 가져갈 수 있다.  
온라인 채점은 **키를 가진 작은 서버**가 하고, HTML은 `guess`만 보낸다.

```text
[브라우저 HTML]
    │  POST { answer, guess }
    ▼
[채점 API]  ← API 키는 여기만
    │  임베딩 + 하이브리드 점수
    ▼
{ score, mode: "online" }
```

오프라인이면 HTML이 지금 `scoreGuess()`를 그대로 쓴다.

---

## 3. 추천 구성

### 3.1 채점 API (필수)

처음엔 이 중 하나면 된다.

| 선택 | 언제 | 비고 |
|------|------|------|
| **Cloudflare Worker** (권장) | 공용 와이파이 | 무료 티어, 배포 쉬움, 키를 시크릿으로 보관 |
| 로컬 Node (`localhost:8787`) | 부스 노트북만 온라인 | HTML은 같은 PC에서 API 호출 |
| Vercel / Cloud Run | 이미 쓰고 있을 때 | 동일 인터페이스면 교체 가능 |

HTML `file://`에서는 외부 HTTPS Worker 호출이 브라우저에 따라 막힐 수 있다.  
**권장:** 행사용은 `npx serve` 등으로 HTML을 `http://localhost`로 열고, API는 Worker 또는 `localhost`로 둔다.

### 3.2 임베딩 모델 (1순위)

**OpenAI `text-embedding-3-small`**

- 품질/가격/안정성 균형
- 정답 벡터는 미리 계산해 캐시
- 입력 단어만 실시간 임베딩

대안: 한국어에 더 잘 맞는 **KR-SBERT / KURE**를 서버에 올리는 것.  
품질은 더 좋을 수 있으나 배포가 무겁다. 1차는 OpenAI, 2차에 교체해도 API 모양은 그대로 둔다.

LLM에게 “몇 점인지 물어보기”는 쓰지 않는다. 느리고 점수가 흔들린다.

---

## 4. 온라인 채점 공식

오프라인 표를 **가드레일**로 남기고, 빈칸만 임베딩이 채운다.

```text
1. 정답 / alias 일치            → 100  (항상, 오프라인과 동일)
2. extras / facet 정확 일치     → 표 점수 그대로
3. 온라인 임베딩 점수            → 아래 변환
4. 오프라인 fallbackScore       → API 실패·타임아웃일 때만
```

### 4.1 정답을 한 덩어리가 아니라 “축 여러 개”로 임베딩

각 정답마다 벡터를 여러 개 만든다.

```text
1905
├─ 문장 A: "1905. 고려대학교 창립 연도, 보성전문학교 개교."
├─ 문장 B: "1905. 고려대학교 중앙 락밴드 동아리."
└─ 문장 C: description 전체
```

입력 `밴드`는 B와 가깝고, `개교`는 A와 가깝다.  
**점수 = 축별 코사인 유사도의 최댓값.**

문장 템플릿은 이미 HTML에 있는 `description` + `facets[].label` + 상위 키워드로 자동 생성한다.  
별도 라벨링을 거의 안 해도 된다.

### 4.2 코사인 → 0~100

생 코사인은 관련 단어가 전부 20~40에 몰린다. 꼬맨틀처럼 **위쪽을 늘린다.**

```text
sim   = max cosine(guess, facetVector)
norm  = (sim + 1) / 2          # 0~1
score = round(100 * norm^1.6)  # 가파르게
```

지수 `1.6`은 플레이테스트로 1.4~2.0 사이 조정.  
`피자`는 낮게, `학과`·`밴드`는 중간~위로 가게 맞춘다.

상한: 임베딩만으로는 **96을 넘지 않음**. 100은 정답/alias만.

### 4.3 하이브리드 (중요)

표에 있는 단어는 오프라인 점수를 **우선**한다.  
온라인은 “표에 없는 단어”와 “표 점수와 큰 차이 없는 보정”에만 쓴다.

```text
if 표에 정확 일치:     표 점수
else if 온라인 성공:
    if 오프라인 부분일치/클러스터 >= 28:  max(온라인, 오프라인)
    else:                               온라인
else:
    오프라인 전체
```

이렇게 하면 `박성빈` 같은 고유 힌트는 표가 지키고, `데이터과학과`는 임베딩이 살린다.

### 4.4 재현성

같은 `(answer, guess)`는 서버 메모리/KV에 캐시한다.  
모델·프롬프트 문장을 바꾸면 캐시 키에 `modelVersion`을 넣는다.

---

## 5. 온라인 여부 판정

`navigator.onLine`만 믿지 않는다. (가짜 true가 많다)

부팅 시·매 판 시작 시:

1. `GET /health` 를 800ms 안에 받는지
2. 실패하면 그 판은 오프라인 모드로 고정
3. 판 중간에 네트워크가 살아도 **그 판은 모드를 바꾸지 않음** (점수 체계가 흔들리면 안 됨)

화면 구석에 작게 `ONLINE` / `OFFLINE`만 표시. 참가자 설명은 필요 없다.

한 단어 채점 타임아웃 **1.2초**. 초과 시 그 단어만 오프라인 점수.

---

## 6. API 계약

```http
POST /score
{
  "answer": "1905",
  "guess": "밴드",
  "stage": "stage2"
}
```

```http
200
{
  "score": 88,
  "mode": "online",
  "facet": "rock-band"
}
```

```http
GET /health
{ "ok": true, "model": "text-embedding-3-small", "v": 1 }
```

서버는 정답 목록을 이미 알고 있다.  
클라이언트가 보낸 `answer`가 목록에 없으면 400.

정답 벡터는 서버 시작 시 전부 만들어 둔다 (1단계 18 + 2단계 20, 축당 2~3개 →  invok 100회 미만).

---

## 7. HTML 쪽 변경 (작음)

지금 `submitGuess()` / 힌트 점수만 갈아끼운다.

```text
async function scoreOf(word):
  if 정답/alias: return 100
  if 이번 판 모드 == offline: return scoreGuess(...)
  try:
    return await apiScore(word)
  catch:
    return scoreGuess(...)
```

힌트 공개 순서와 랭킹 점수식은 그대로 둔다.  
바꾸는 것은 **단어 유사도 %** 뿐이다.

설정 한 줄:

```js
const SCORE_API = 'https://xxx.workers.dev'; // 또는 '' 이면 항상 오프라인
```

---

## 8. 학과 특화 데이터 (서버가 씀)

정답별로 임베딩 문장을 고정한다. 예:

| 정답 | 축 문장 |
|------|---------|
| 컴데인 | 고려대 정보대학 학술 전시 행사. 컴퓨터학과·데이터과학과·인공지능학과 프로젝트. |
| 1905 | (연도) / (락밴드) 두 문장 |
| 펄스 | 고려대 정보대학 학생회. 정보인의날·학과 행사 운영. |
| 고인돌 | 인공지능학과 노션 꿀팁 공유 페이지. |

추가로 **공통 문맥 접두어**를 모든 문장 앞에 붙인다.

```text
고려대학교 안암캠퍼스 정보대학 인공지능학과 맥락:
```

일반 임베딩이 `크림슨`을 색깔로만 보지 않고 학교 상징으로 기울게 한다.

이 문장 표는 `운영진-정답목록.md` 옆에 `임베딩-문장.md`로 두고, 서버가 그걸 읽게 한다.

---

## 9. 비용·운영 (정보인의날 하루)

가정: 참가 80명 × 평균 입력 12회 ≈ 1000회 임베딩.

`text-embedding-3-small` 기준 하루 **수백 원~수천 원** 수준.  
정답 프리컴퓨팅은 무시 가능. 입력 캐시가 있으면 더 줄어든다.

운영 체크리스트:

- [ ] Worker 배포, 시크릿에 OpenAI 키
- [ ] 부스 노트북에서 `/health` 확인
- [ ] 실패 시 HTML만으로도 시작 가능한지 확인
- [ ] 모드 표시가 ONLINE인지 보고 시작

---

## 10. 구현 순서

### 1단계 — 인터페이스만 (반나절)

- `SCORE_API` 빈 값이면 지금과 100% 동일
- `ONLINE/OFFLINE` 표시
- `/health` 실패 시 오프라인

### 2단계 — 채점 Worker (하루)

- `/score`, 캐시, 정답 축 문장
- 정답/alias/표 키워드는 서버에서도 동일 규칙으로 100 또는 표 점수

### 3단계 — HTML 연결 (반나절)

- `submitGuess` / 힌트만 async
- 검색 버튼 누르는 동안 짧게 대기 (기존 누름 애니메이션 유지)

### 4단계 — 플레이테스트 (필수)

고정 입력으로 점수표를 뽑는다.

| 정답 | 입력 | 기대 |
|------|------|------|
| 컴데인 | 데이터과학과 | 40~70 |
| 컴데인 | 피자 | 0~15 |
| 1905 | 밴드 | 75+ |
| 1905 | 개교 | 80+ |
| 챗GPT | 제미나이 | 표 점수(88) 유지 |
| 호랑이 | 마스코트 | 표 점수 유지 |
| 펄스 | pulse | 100 |

기대와 어긋나면 지수(1.6) 또는 축 문장만 고친다. 표는 함부로 버리지 않는다.

### 5단계 — 행사 당일

- 기본은 온라인
- 먹통이면 `SCORE_API = ''` 이거나 health 실패로 자동 오프라인

---

## 11. 결정이 필요한 것

구현 들어가기 전에 이것만 정하면 된다.

### P1. API 위치

- [ ] Cloudflare Worker (권장)
- [ ] 부스 노트북 localhost

### P2. 모델

- [ ] OpenAI `text-embedding-3-small` (권장, 1차)
- [ ] 나중에 한국어 SBERT로 교체 가능하게만 추상화

### P3. 온라인 점수와 표 점수

- [x] 권장: 표 정확 일치는 표 우선, 나머지 온라인, 실패 시 오프라인
- [ ] 대안: 온라인이 되면 표는 alias/100만 쓰고 전부 임베딩

### P4. 행사 HTML 여는 방법

- [ ] `npx serve` / VS Code Live Server 로 localhost (권장)
- [ ] `file://` 유지 — 이 경우 Worker CORS + 브라우저 제한을 더 봐야 함

---

## 12. 결론

- **오프라인:** 지금 facet + 클러스터 + 해시 (이미 동작)
- **온라인:** 정답 축 문장 임베딩 최댓값 + 표를 가드레일로
- **연결:** 작은 `/score` API, 키는 서버만, 한 판의 모드는 고정
- **실패:** 단어 단위 1.2초 타임아웃 → 그 단어만 오프라인

P1~P4를 정하면 2단계 Worker부터 구현에 들어가면 된다.
