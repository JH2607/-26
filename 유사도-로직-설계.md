# AI 심문실 — 유사도 로직 설계

> 목적: 고려대 인공지능학과 부스용 꼬맨틀형 오프라인 게임의 유사도 채점 방식을 확정한다.  
> 제약: **인터넷 없이** 단일 HTML로 동작해야 한다. (임베딩 API / LLM 호출 불가)

---

## 1. 문제 정의

### 현재 방식의 한계

지금 로직은 **정답별 단어→점수 룩업 테이블** + 문자열 부분일치 + 미등록 단어 의사난수다.


| 방식        | 역할                 | 한계                          |
| --------- | ------------------ | --------------------------- |
| 룩업 테이블    | 미리 넣은 단어만 정확한 점수   | 관련 단어를 빠뜨리면 점수가 낮음          |
| 부분 문자열 매칭 | `"크림"` → `"크림슨"` 등 | 의미 관계는 못 잡음 (`밴드` ↔ `1905`) |
| 해시 의사난수   | 미등록 단어에 -25~20     | “관련 없어 보임”만 표현, 힌트가 안 됨     |


### 핵심 사례: `1905`

`1905`는 동시에:

1. **고려대학교 창립 연도** (보성전문학교 1905)
2. **고려대 중앙 락밴드 동아리** 이름

참가자가 `밴드`를 넣으면 “동아리 축”으로 높은 유사도가 나와야 하는데, 테이블에 `밴드`가 없으면 낮은 점수가 나온다.  
즉 **한 정답이 여러 의미 축(facet)** 을 가질 때, 단순 단어 목록만으로는 관리·확장이 어렵다.

---



## 2. 설계 목표

1. **다의어/다면 정답**을 자연스럽게 표현할 수 있을 것 (`1905` = 연도 + 밴드)
2. 스태프가 **새 단어·힌트를 추가하기 쉬울** 것 (표만 늘리는 방식보다 구조화)
3. 점수가 **재현 가능**할 것 (같은 입력 → 항상 같은 점수)
4. **오프라인·클라이언트만**으로 동작
5. 참가자 입장에서 “이 방향으로 가면 되겠다”는 **탐색 피드백**이 생길 것

비목표:

- 실제 NLP 임베딩으로 “세상의 모든 유사도”를 exact하게 재현하는 것
- 정답 유출 방지(정답은 이미 클라이언트에 존재). 보안이 아니라 **플레이 품질**이 목표

---



## 3. 접근 비교



### A. 지금처럼 평면 룩업만 유지

- 장점: 구현 단순, 점수 완전 통제
- 단점: `밴드`처럼 빠진 키워드마다 수동 추가. 다의어는 “그냥 더 긴 리스트”로만 해결 → 유지보수 지옥



### B. 정답마다 설명문 + 키워드 백만 두기 (채점 규칙은 모호)

- 장점: 문서화에는 좋음
- 단점: “설명과 얼마나 비슷한가”를 코드가 자동으로 못 잼 (오프라인). 결국 다시 수동 점수가 필요



### C. **특징(facet) + 키워드 그룹** 방식 ← 권장

정답을 “한 뭉치 단어표”가 아니라 **의미 축(facet)** 들의 집합으로 모델링한다.

```text
정답: 1905
├─ facet "창립연도" (축 중요도 높음)
│    keywords: 개교, 창립, 연도, 보성전문학교, 1946, ...
│    near:     역사, 전통, 사학, ...
└─ facet "락밴드 동아리"
     keywords: 밴드, 락밴드, 동아리, 공연, 라이브, ...
     near:     음악, 기타, 보컬, ...
```

- 장점
  - `밴드`가 어느 축에 속하는지 명확
  - 축마다 점수 상한/힌트 순서를 다르게 줄 수 있음
  - 나중에 설명 문장(`description`)을 붙여 스태프 가이드로 쓰기 좋음
- 단점
  - 초기 데이터 작성 비용이 있음 (한 번 구조를 잡으면 이후는 쉬움)



### D. 동의어 사전을 전역으로 두기

예: `밴드 ≈ 락밴드 ≈ 그룹사운드`

- 장점: 여러 정답에 재사용
- 단점: `밴드`가 다른 정답(예: 응원단 밴드?)과 충돌할 수 있음 → **전역 동의어는 보조**, 정답별 facet 키워드가 본체

---



## 4. 확정안 (초안) — Facet + Tier 채점



### 4.1 데이터 스키마

각 정답 세트는 아래 형태를 따른다.

```js
{
  answer: "1905",
  aliases: ["일구공오"],           // 입력 시 100% 정답 처리
  description: "고려대 창립 연도이자 중앙 락밴드 동아리",  // 스태프/문서용, 채점 미사용 가능
  facets: [
    {
      id: "founding-year",
      label: "창립 연도",
      // 이 축으로 다가가면 줄 점수 구간
      tiers: {
        core:  { score: 90, words: ["개교", "창립", "개교년도", "보성전문학교"] },
        strong:{ score: 78, words: ["설립", "연도", "개교기념", "1946"] },
        weak:  { score: 55, words: ["역사", "전통", "사학", "민족대학"] }
      }
    },
    {
      id: "rock-band",
      label: "락밴드 동아리",
      tiers: {
        core:  { score: 92, words: ["밴드", "락밴드", "동아리밴드"] },
        strong:{ score: 80, words: ["동아리", "공연", "라이브", "중앙동아리"] },
        weak:  { score: 50, words: ["음악", "기타", "보컬", "합주"] }
      }
    }
  ],
  // facet에 넣기 애매한 단독 고득점 단어 (교수명, 고유 표현 등)
  extras: {
    "고려대": 67,
    "안암": 30
  },
  // 힌트 버튼용: 낮은 점수부터 공개 (기존과 동일 정책)
  hints: ["연도", "보성전문학교", "밴드", "락밴드"]
}
```



### 4.2 점수 우선순위 (확정 후보)

입력 `w`에 대해 (공백 제거 정규화 후):

1. **정답 / alias 일치** → `100`
2. **extras 정확 일치** → 해당 점수
3. **facet tier 정확 일치** → 해당 tier 점수
  - 여러 facet/tier에 걸리면 **최댓값**
4. **부분 문자열 매칭** (길이 ≥ 2)
  - 정답 문자열과의 포함 관계 → 기존처럼 65~95 (비율 기반) **또는** facet 키워드와의 포함이면 `floor(tierScore * 0.9)`
  - extras 키와의 포함 → `floor(extraScore * 0.85)`
5. **어느 축에도 안 걸림** → 고정 의사난수 `-20 ~ 15` (관련 없어 보이게)

원칙:

- **의미는 facet 키워드로만** 올린다. 추측성 자동 확장은 하지 않는다.
- 같은 입력이 두 축에 걸리면 **높은 쪽**만 채택 (참가자에게는 “어느 축이든 가까이 가면 점수 상승”으로 보임).



### 4.3 왜 “설명문 임베딩”이 아닌가

부스 환경은 오프라인이라 문장 유사도를 계산할 모델이 없다.  
따라서 **설명문(**`description`**)은 사람용 메타데이터**로 두고, 실제 채점은 **사람이 고른 키워드 tier**로 한다.

설명문을 쓰는 경우의 올바른 역할:

- 스태프가 facet/키워드를 빠짐없이 채우도록 돕는 **작성 가이드**
- (선택) 결과 화면에서 “정답 해설” 표시



### 4.4 힌트 정책 (현행 유지 + 정리)

- 힌트 풀은 **강한 단서만** (대략 70+에 해당하는 core/strong)
- **점수 오름차순**으로 하나씩 공개
- 이미 참가자가 직접 맞춘 단어는 스킵
- UI 문구와 실제 감점 일치 필요: 현재 버튼은 `-150`, 계산은 `-60` → 구현 시 **하나로 통일** (제안: 감점 60, 문구도 `-60점`)



### 4.5 2단계 랭킹 점수식 (현행 유지 제안)

```
score = max(0, round(1000 - tries*60 - elapsedSec*2.5 - hints*60))
```

등급 조건(S/A/B)은 기존과 동일. 유사도 로직만 교체해도 행사 운영은 그대로다.

---



## 5. `1905` 적용 예시 (기대 동작)


| 입력       | 기대 점수대 | 이유                   |
| -------- | ------ | -------------------- |
| `1905`   | 100    | 정답                   |
| `밴드`     | ~92    | rock-band / core     |
| `락밴드`    | ~92    | rock-band / core     |
| `동아리`    | ~80    | rock-band / strong   |
| `개교`     | ~90    | founding-year / core |
| `보성전문학교` | ~90    | founding-year / core |
| `역사`     | ~55    | founding-year / weak |
| `음악`     | ~50    | rock-band / weak     |
| `피자`     | -20~15 | 미등록                  |


→ “연도 축”으로 가도, “밴드 축”으로 가도 점수가 오르고, 두 축이 만나면 정답에 수렴.

---



## 6. 작업 순서 (제안)

1. [x] 이 문서의 **스키마·우선순위** 합의 (숫자 미세조정은 나중에)
2. [x] 기존 `DATA.stage1/2` 정답마다 `description` + `facets` 초안 작성 (`정보인의날_AI심문실.html` DATA)
3. [x] `scoreGuess`를 facet/tier 기반으로 교체
4. [ ] `1905`, `펄스`, `정이정정` 등 다의어부터 플레이테스트 (부스 전 수동 확인)
5. [x] 힌트 문구/감점 불일치 수정 (-60점 통일)
6. [x] 스태프용: 셀렉트에 정답 노출 유지

---



## 7. 결정이 필요한 항목

아래만 정하면 구현에 들어갈 수 있다.

### D1. 스키마

- [x] 권장: **Facet + Tier (core/strong/weak)** + extras + aliases  
- [ ] 대안: 평면 룩업만 보강 (비권장)



### D2. 부분 문자열

- [ ] A: facet 키워드에만 부분일치 허용  
- [x] B: 정답 문자열 부분일치도 유지 (현행, `"크림"`→`"크림슨"`에 유리)

**제안:** B 유지 (한국어 고유명 축약에 유용)

### D3. 미등록 단어 점수 범위

- [ ] `-25 ~ 20` (현행)  
- [x] `-20 ~ 15` (관련 없음이 더 명확)

**제안:** `-20 ~ 15`

### D4. 힌트 감점

- [x] 60점 (계산식과 통일)  
- [ ] 150점 (버튼 문구에 맞춤, 식도 변경)

**제안:** **60점으로 통일**

### D5. description 노출

- [ ] 스태프 설정 화면에만 표시  
- [ ] 정답 공개(결과) 화면에 해설로 표시  
- [ ] 채점/UI 모두 미사용, 문서용만

**제안:** 결과 화면에 한 줄 해설 + 데이터 파일에도 유지(이렇게 확정할게.)

---



## 8. 결론

- “설명 + 키워드를 미리 정리”하는 방향은 **맞다.**  
다만 설명문 자체가 점수를 만드는 게 아니라, **설명을 기준으로 facet/키워드 tier를 사람이 채점표로 고정**하는 방식이 오프라인 부스에 맞다.
- 유사도 로직 확정 후보:
  > **정규화 → 정답/alias → extras → facet tier 매칭(최대값) → 부분일치 → 의사난수**

다음 단계: D2~D5를 고른 뒤, 정답별 facet 데이터 초안 MD/JSON을 만들면 코드 수정으로 넘어간다.